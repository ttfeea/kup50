# Application
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173
# For multiple deployed origins, use a comma-separated list instead.
# FRONTEND_URLS=https://app.example.com,https://preview.example.com

# PostgreSQL (Prisma)
DATABASE_URL=postgresql://postgres:password@localhost:5432/kup50_app

# Auth
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=60d
